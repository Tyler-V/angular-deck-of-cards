import { CorrectPlayerOrderPipe } from './correct-player-order.pipe';

describe('CorrectPlayerOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new CorrectPlayerOrderPipe();
    expect(pipe).toBeTruthy();
  });
});

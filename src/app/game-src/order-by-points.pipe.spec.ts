import { OrderByPointsPipe } from './order-by-points.pipe';

describe('OrderByPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
